base {
  log_debug = off;
  log_info = on;
  log = "stderr";
  daemon = off;
  redirector = iptables;
}

redsocks {
  local_ip = ${redsocks_ip};
  local_port = ${redsocks_port};

  type = ${redsocks_type};
  ip = ${redsocks_proxy_ip};
  port = ${redsocks_proxy_port};
}
